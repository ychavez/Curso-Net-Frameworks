@model IEnumerable<EscuelaMVC.Models.AlumnoCurso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>@Html.DisplayNameFor(x => x.Alumno.Nombre)  </th>
        <th>@Html.DisplayNameFor(x => x.Curso.Nombre)  </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@Html.DisplayFor(x => item.Alumno.Nombre)</td>
        <td>@Html.DisplayFor(x => item.Curso.Nombre)</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
